type Book {
  title: String
  author: String
}

type Profile {
  userName: String
}

type Query {
  books: [Book]
  profile: Profile
}

type Mutation {
  createProfile(userName: String!): Profile
  deleteProfile: Boolean
}

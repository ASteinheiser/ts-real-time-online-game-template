import { describe, it, expect } from 'vitest';
import { StartGameForm } from '../src/renderer/src/components/StartGameForm';

describe('StartGameForm', () => {
  it('should exist', () => {
    expect(StartGameForm).toBeDefined();
  });
});

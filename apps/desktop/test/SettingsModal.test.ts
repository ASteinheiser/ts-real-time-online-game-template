import { describe, it, expect } from 'vitest';
import { SettingsModal } from '../src/renderer/src/modals/SettingsModal';

describe('SettingsModal', () => {
  it('should exist', () => {
    expect(SettingsModal).toBeDefined();
  });
});

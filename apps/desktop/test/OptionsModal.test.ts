import { describe, it, expect } from 'vitest';
import { OptionsModal } from '../src/renderer/src/components/OptionsModal';

describe('OptionsModal', () => {
  it('should exist', () => {
    expect(OptionsModal).toBeDefined();
  });
});
